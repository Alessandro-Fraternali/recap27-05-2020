$(document).ready(function() {

  var a = 1;

  var pory = (a > 5) ? true : false;
  // uso il ? per chiedere se a è >5 e se lo è pory diventa true
  // uso il : per dire che se a non è >5 allora pory diventa false

  console.log(pory);

 })
